/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',  // Set the output to static export
    trailingSlash: true,  // Optional: Adds trailing slashes to URLs for better compatibility with static hosting
};

export default nextConfig;
  